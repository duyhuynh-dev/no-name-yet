# Core Dependencies
# Note: Python 3.8-3.11 is required (specified in README)
# The Python version is determined by your virtual environment

# Reinforcement Learning
gymnasium>=0.29.0
stable-baselines3>=2.1.0
torch>=2.0.0

# Data Processing
pandas>=1.5.0,<2.0.0  # pandas 2.0+ requires Python 3.10+
numpy>=1.24.0,<2.0.0  # numpy 2.0+ requires Python 3.10+

# Technical Analysis
TA-Lib>=0.4.28
# pandas-ta  # Alternative if TA-Lib installation fails (install separately if needed)

# Data Sources
yfinance>=0.2.28
ccxt>=4.0.0  # For crypto data

# MLOps & Experiment Tracking
mlflow>=2.8.0

# API & Web Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# Utilities
python-dotenv>=1.0.0
tqdm>=4.66.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# TensorBoard (for training visualization)
tensorboard>=2.14.0

# Code Quality
black>=23.10.0
flake8>=6.1.0
mypy>=1.6.0

# Optional: Distributed Training (for future use)
# ray[default]>=2.8.0
# ray[rllib]>=2.8.0

# Optional: Hyperparameter Tuning
# optuna>=3.4.0
# ray[tune]>=2.8.0

# Optional: Data Versioning
# dvc>=3.38.0

pytest-asyncio
